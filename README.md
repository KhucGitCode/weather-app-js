# 🌦️ Weather App (JavaScript)

A simple and responsive **Weather Application** built using **HTML, CSS, and Vanilla JavaScript**.  
The app fetches real-time weather data from the **OpenWeather API** and displays current weather information for any searched city.

---


## 📌 Project Overview

This Weather App allows users to search for a city and view its current weather conditions.  
The application demonstrates real-world usage of **REST APIs**, **async/await**, and **error handling** in JavaScript.

The project was built as part of a hands-on learning journey to strengthen **API integration and asynchronous JavaScript concepts**.

---

## 🛠️ Tech Stack

- **HTML5**
- **CSS3**
- **JavaScript (ES6)**
- **OpenWeather API**

---

## Features

-  Search weather by city name
-  Displays temperature in Celsius
-  Shows humidity level
-  Weather condition description
-  Dynamic weather icons
-  Enter key support for search
-  Error handling for invalid city names
-  Loading state while fetching data

---

##  How It Works

1. User enters a city name
2. App sends a request to the OpenWeather API
3. Weather data is fetched using `fetch` with `async/await`
4. UI updates dynamically with:
   - City name
   - Temperature
   - Humidity
   - Weather condition & icon
